apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: dev-research-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: research-app
      component: frontend
  template:
    metadata:
      labels:
        app: research-app
        component: frontend
        tier: web
        environment: dev
    spec:
      containers:
        - name: frontend
          image: lukky175/research-summarizer-frontend:6a343a70b2654e2975027b2e2871ae1c7e632f2c
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
